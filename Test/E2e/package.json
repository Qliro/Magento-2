{
  "name": "qliroone-e2e",
  "private": true,
  "description": "Browser end to end tests for the QliroOne module against a local Magento",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
